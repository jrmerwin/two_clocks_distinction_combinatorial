# Reproducibility statement

Run environment:

```bash
cd ~/deu_class/CLASS-DEU
source ~/deu_class/deu_boltzmann_start/.venv/bin/activate
export COBAYA_PACKAGES_PATH=~/deu_class/cobaya_packages
```

Core lineage commands are represented by booked stage scripts under `deu_validation/` and outputs under `deu_validation/stage3b_outputs/`.
This preprint package does not launch MCMC and does not compute new evidence; it assembles already-booked artifacts.

Reviewer/external-validation branch note:

The external reviewer branch was not used for this author-led preprint path. The manuscript must not claim peer review or independent external review.
